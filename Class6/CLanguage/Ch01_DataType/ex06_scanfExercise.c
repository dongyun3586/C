#include<stdio.h>

int main_ex06_scanfExercise() {
	//필요한 변수 선언
	int a, b, c;

	// a, b에 정수값 입력받기
	printf("정수값 두 개 입력: ");
	scanf("%d %d", &a, &b);

	// a, b값 출력
	printf("a=%d, b=%d \n", a, b);

	// a, b값 교환

	// a, b값 출력
	printf("a=%d, b=%d \n", a, b);
}