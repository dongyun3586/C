#include <stdio.h>

/* 정수 뒤집어서 출력하기 */
int main() {
    int n1, n2 = 0, ja = 1, di = 1;

    printf("입력 정수: ");
    scanf("%d", &n1);

   /* for (ja = 1; n1 != 0; ja++)
        n1 /= 10;
    for (int i = 1; i <= ja; i++;)
        m *= 10;
    for (int i = 1; i <= ja; i++;) {
        n2+=
    }
    */

    return 0;
}